# SGF Lexicon Pipeline -- Python dependencies
#
# Install with:    pip install -r requirements.txt
#
# IMPORTANT: pick ONE onnxruntime variant matching your hardware
# and comment out the other two. Installing more than one causes
# silent conflicts that produce wrong embeddings.

# ----- ONNX runtime: pick ONE -----
onnxruntime           # CPU only (works everywhere)
# onnxruntime-gpu     # NVIDIA GPU (uncomment, comment the line above)
# onnxruntime-directml  # Windows + AMD / Intel iGPU (uncomment, comment the CPU line)

# ----- The rest (no choice involved) -----
tokenizers
huggingface_hub
numpy

# ----- Optional, for v1.1 FAISS swap-in (large lexicons) -----
# v1 uses NumPy batched matmul, which is enough for <200K senses.
# Uncomment when scaling beyond that.
# faiss-cpu

# ----- Optional, for running glean_search_server.py (the HTTP daemon) -----
# Not required for the bootstrap pipeline; only needed if you want to
# stand up the search server. The bootstrap's Stage 11 resolver imports
# lexicon_search.py in-process and does NOT require these.
# fastapi
# uvicorn
# pydantic

# ----- TOML parsing on Python < 3.11 -----
# tomli; python_version < "3.11"
